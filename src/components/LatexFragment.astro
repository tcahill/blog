---
import katex from 'katex';

const { node } = Astro.props;
const match = node.properties.value.match(/^\$(.*)\$$/);
const value = match[1] || node.properties.value;
const html = katex.renderToString(value, { output: 'html' });
---

<Fragment set:html={html} />
