---
const spaceNodeTypes = ['entity', 'italic', 'link', 'verbatim'];

const { node } = Astro.props;

const spaces = spaceNodeTypes.includes(node.type)
? " ".repeat(node.properties.postBlank)
: "<br />".repeat(Math.max(0, node.properties.postBlank - 1))
---
<Fragment set:html={spaces} />
